# v0.8.1 CAD 预览真实样本说明

本目录用于放置 CAD 预览真实或接近真实的兼容性测试样本。

建议准备但不要提交真实涉密图纸：

- 普通 DXF。
- 中文文字 DXF。
- 大范围坐标 DXF。
- 图形很小或很偏的 DXF。
- 多图层 DXF。
- 含 TEXT / MTEXT 的 DXF。
- 含 INSERT / BLOCK 的 DXF。
- 含 CIRCLE / ARC / POLYLINE 的 DXF。
- 由 DWG 转换得到的 DXF。
- 损坏或异常 DXF。

真实图纸文件应通过 `.gitignore` 排除，只保留本说明文件。
